# בריף: חיבור אתר הקורסים לאתר הראשי + דף אודות

נכתב 29.7.2026 בסוף סשן ההשקה, **כדי שהסשן הבא לא ישאל את הילה שאלות שכבר יש להן תשובה**. כל מה שכתוב כאן תחת "מצב קיים" נבדק בפועל מול הקוד, מול בסיס הנתונים ומול האתר החי — לא הוסק ולא שוער.

**המשימה, בשתי שורות:**
1. לחבר בין `www.ganani.co.il` (וורדפרס) לבין `courses.ganani.co.il` (Lovable). כרגע הם מנותקים לחלוטין, בשני הכיוונים.
2. ליצור דף אודות באתר הקורסים. כרגע אין.

---

## 1. איך ניגשים

| | |
|---|---|
| פרויקט Lovable | `gananicourses` · `28be939f-66fe-4dbb-8b88-fbebbaa74151` |
| Workspace | `SDhpfFncRNZhwHGbc7jS` |
| פרודקשן | `https://courses.ganani.co.il` |
| Stack | TanStack Start (TypeScript) + Supabase |

**קריאה** — דרך ה-MCP של Lovable (`read_file` / `list_files` / `query_database`). אין עותק מקומי.

⚠️ **כתיבה עובדת אחרת: אי אפשר לערוך קבצים ישירות.** שינוי קוד נעשה ע"י שליחת הודעה בשפה טבעית לסוכן של Lovable (`send_message`), **וזה צורך קרדיטים מהחשבון של הילה**. המשמעות המעשית: לגבש בריף אחד מדויק ומלא לפני ששולחים, לא לגשש בסדרת הודעות. שווה גם לשקול `plan_mode=true` לדיון לפני כתיבת קוד.

רקע כללי על הפרויקט: [`courses-platform.md`](courses-platform.md).

---

## 2. מצב קיים — נבדק, לא שוער

### האתר הראשי (וורדפרס, `www.ganani.co.il`)

תפריט ראשי: **אודות** (`/hila-ganani/`) · תכנון מסע לקוח · ניהול קמפיינים · לקוחות ממליצים · בלוג · צרו קשר.

**אין בו שום קישור לאתר הקורסים.** לא בתפריט ולא כפריט משנה.

### אתר הקורסים (Lovable)

**הדר** (`src/components/site-header.tsx`) — ניווט: `/` ("קורסים") · `/contact` · `/account` (למחוברים) · `/admin` (לאדמין). **אפס קישורים לאתר הראשי.**

**פוטר** (`src/components/site-footer.tsx`) — לוגו, קישור וואטסאפ, ואז המחרוזת `· ganani.co.il` **כטקסט חופשי, לא כקישור `<a>`**. זה כנראה התיקון הקל והמשמעותי ביותר בכל המשימה.

*(הערה: אותו פוטר החזיק גם קישור שבור באייקון האתר בתבנית הניוזלטר בסמוב — עניין נפרד, תוקן 29.7, ראה `launch-7-yesodot.md` §7.)*

### דף אודות — לא קיים בשום צורה

- אין `src/routes/about.tsx`
- אין שורה `about` בטבלת `site_pages` (יש בה בדיוק ארבע: `accessibility` · `contact` · `privacy` · `terms`)

**החומר הקרוב ביותר שכבר קיים:** סעיף "למה איתי?" בדף הקורס (`src/routes/course.$slug.tsx`) — פסקה על 20 שנה ומאות עסקים, תגית "20 שנות ניסיון", ותגית "5 כוכבים בגוגל · 23 ביקורות". זה טקסט מכירתי בתוך דף מוצר, לא דף אודות.

---

## 3. הדפוס המדויק ליצירת דף חדש

עמוד תוכן באתר הזה מורכב מ**ארבעה חלקים**, ואם מפספסים אחד הוא נשבר בשקט:

1. **שורה ב-`site_pages`** — `slug`, `title`, `content`. הטבלה היא `id · slug · title · content · updated_at · created_at`. **אין בה `is_published`** — כל שורה קיימת היא פומבית.
2. **קובץ route** — `src/routes/about.tsx`. להעתיק את המבנה מ-`src/routes/terms.tsx`: `head()` עם title/description/og/canonical דרך `appUrl()`, ואז `useQuery` על `site_pages` לפי ה-slug ורינדור עם `parsePageContent`. פורמט התוכן: טקסט חופשי, ו-`## ` בתחילת שורה = כותרת משנה.
3. **`PUBLIC_PATHS` ב-`src/routes/_authenticated/admin/pages.tsx`** — מפה קשיחה של slug→נתיב. בלי `about: "/about"` כפתור התצוגה המקדימה בממשק הניהול יוביל לדף הבית.
4. **`src/routes/sitemap[.]xml.ts`** — מערך `entries` **קשיח**. דף שלא נוסף שם לא ייכנס ל-sitemap. *(שימו לב: `/contact` כבר חסר שם היום — פער קיים, שווה לתקן באותה הזדמנות.)*

**ממשק הניהול יודע לערוך עמודים קיימים בלבד, לא ליצור חדשים.** אין בו כפתור "עמוד חדש". כלומר את השורה הראשונית ב-`site_pages` צריך ליצור ב-SQL או דרך הסוכן של Lovable; אחרי שהיא קיימת, הילה תוכל לערוך את התוכן בעצמה ב-`/admin/pages`.

---

## 4. מה צריך להכריע — לשאול את הילה רק את אלה

1. **כיוון החיבור ומיקומו.** להוסיף "קורסים" לתפריט הראשי בוורדפרס? מי עורך את הוורדפרס בפועל — היא או מישהו אחר? ומה חוזר מכיוון הקורסים: קישור בפוטר בלבד, או גם פריט בהדר?
2. **תוכן דף האודות.** לשכתב מחדש, או לעבד את הטקסט מ-`/hila-ganani/`? ⚠️ **העתקה מילה במילה היא בעיה** — תת-דומיין נחשב אתר נפרד לגוגל, ותוכן זהה בשני מקומות יוצר כפילות. אם רוצים בכל זאת את אותו טקסט, צריך `canonical` שמצביע לגרסה באתר הראשי, וזה מוותר על הדירוג של הגרסה בקורסים.
3. **מה התפקיד של דף האודות כאן.** אודות באתר קורסים הוא בדרך כלל כלי המרה ("למה ללמוד ממנה"), לא ביוגרפיה — והוא שונה מהאודות של אתר השירותים. שווה לוודא איתה שזו הכוונה לפני שכותבים.
4. **האם הדף אמור להיות מאונדקס** ולהיכנס ל-sitemap.

---

## 5. מלכודות שכבר ידועות

- **אין שום אנליטיקס באתר הקורסים.** אין GA4, GTM, פיקסל, כלום — נבדק על האתר החי (`courses-platform.md`). המשמעות למשימה הזו: **אי אפשר יהיה למדוד אם החיבור בין האתרים עובד.** אם מדידה חשובה, GA4 הוא תנאי מקדים ולא שיפור עתידי.
- **תיוג קישורים לפי יעד, לא לפי מקור.** קישור מהוורדפרס לאתר הקורסים הוא חוצה-דומיין ולכן **צריך UTM**; קישור פנימי בתוך אותו אתר **בלי שום תיוג**; `?cs=` שמור אך ורק ליעדים בסמוב (`vp4.me`). הכלל המלא ב-`content-production-checklist.md` §6. זה בדיוק מה שנשבר ב-26 קישורים במיילי ההשקה.
- **לא לגעת בתזרים התשלום.** `finalizeOrder` ו-`src/lib/sumit.server.ts` נשברו בעבר, תוקנו ונבדקו end-to-end. ההשקה חיה — מייל ראשון יצא 29.7 ומיילי מכירה יוצאים מ-20.8.
- **`courses.ganani.co.il` הוא תת-דומיין נפרד מ-`www.ganani.co.il`** לכל דבר ועניין בגוגל: אין העברת סמכות אוטומטית, וקישור ביניהם הוא קישור חיצוני.

---

## 6. הצעה לסדר עבודה

הקל והבטוח קודם, היקר והמסוכן אחרון:

1. **להפוך את `ganani.co.il` בפוטר לקישור אמיתי.** שינוי של שורה אחת, ומחזיר את רוב הערך של "חיבור בין האתרים".
2. **להחליט על תוכן האודות מול הילה** — לפני שנוגעים בקוד, כי זו ההחלטה שמכתיבה את כל השאר.
3. **ליצור את דף האודות** — ארבעת החלקים מ-§3 בהודעה אחת מגובשת לסוכן של Lovable.
4. **קישור מהוורדפרס לקורסים** — דורש גישה לוורדפרס, שלא נבדקה. לברר מי מבצע.
