# קליטת לידים ל-CRM — עקרונות עבודה

איך ליד שנכנס בוואטסאפ או באינסטגרם הופך לרשומה מלאה ב-CRM. המסמך הזה הוא מקור האמת לתהליך — לא ל-CRM עצמו, שהוא בייס Airtable נפרד (פרטים למטה).

## איפה ה-CRM יושב

יש **שני** בייסים נפרדים ב-Airtable, וקל להתבלבל ביניהם:

| בייס | ID | מה יש בו |
|---|---|---|
| `ganani.co.il` | `appIgWywUrIU1jcHy` | תוכן: מאגר תוכן קיים, ניוזלטר, דפי נחיתה, תכנון תוכן חודשי |
| **Mini CRM** | `appcfuP4ufqhwipBm` | **לידים, עסקאות, חברות, משימות, מוצרים, תקציב פרסום** |

`AIRTABLE_BASE_ID` ב-`.env` מצביע על בייס התוכן, וכל סקילי ה-Airtable בפרויקט (`airtable-read`, `airtable-write`, `airtable-content-calendar`) מוגדרים עליו בלבד. **הם לא מכירים את ה-CRM.** עבודה מול ה-CRM נעשית בקריאות REST ישירות עם ה-PAT.

טבלאות ה-CRM:

| טבלה | ID |
|---|---|
| אנשי קשר | `tblwIo1FYWq1D8hQw` |
| חברות | `tblVj9TLlnezczLQC` |
| עסקאות | `tbl3dP8utT9rgw0AT` |
| משימות | `tblo7vKrFG8fPaPqb` |
| מוצרים | `tblVEXRxB0BfX1jh9` |
| ניהול תקציב פרסום | `tblixwnxQXpsAefeC` |

**גישה:** ה-PAT שב-`.env` (`AIRTABLE_API_KEY`) רואה את שני הבייסים ומחזיק הרשאות קריאה, כתיבת רשומות, ו-`schema.bases:write`. **חיבור ה-MCP של Airtable לא רואה את בייס ה-CRM** — הוא מחזיר 403. זו לא תקלה זמנית להתעכב עליה: פשוט לעבוד ב-REST.

## ליד אחד = שתי רשומות, לפעמים שלוש

זו הטעות הכי קלה לעשות. רשומה בטבלת **אנשי קשר** לבדה **לא מופיעה בפייפליין בכלל** — שלב המכירה (`Stage`) יושב על **עסקאות**, ומשתקף חזרה לאיש הקשר רק דרך שדה ה-lookup `Stage (from עסקאות)`.

לכן קליטת ליד היא תמיד:

1. **איש קשר** — הבן אדם.
2. **עסקה** — מקושרת אליו דרך שדה `אנשי קשר`. בלעדיה הליד בלתי נראה.
3. **חברה** — כשידוע איפה הוא עובד. מקושרת גם לאיש הקשר וגם לעסקה.

`Opportunity Name` בעסקה הוא נוסחה: `<איש קשר> | <תיאור>`. אין מה למלא אותו ידנית.

## כללי שדות שאסור לפספס

**`תאריך קליטה` — תמיד.** תצוגת ברירת המחדל של הילה ממוינת לפיו. רשומה בלי תאריך צונחת לתחתית הטבלה והליד החדש פשוט לא נראה.

**`מקור הגעה` — תמיד במפורש.** לשדה יש **ברירת מחדל של "דף נחיתה"** ברמת הטבלה. רשומה שנוצרת בלי השדה יוצאת מתויגת כליד מדף נחיתה גם אם היא הגיעה ממקום אחר לגמרי — נתון שגוי שנראה תקין.

**`Stage` — `נוצר קשר 🤝` עד שיוצאת הצעת מחיר.** ההגדרה של הילה: דיברתי או התכתבתי, עדיין לא נשלח מחיר. פגישה שנקבעה היא **עדיין** `נוצר קשר` — לא `פולואפ 📅`. היא מנהלת את הפייפליין לפי "יצא מחיר או לא", כי זו השאלה שבאמת מנבאת הכנסה.

**`סיכום לקראת שיחה`** (בעסקאות) — שדה טקסט ארוך לתדריך הכנה לשיחה. נוצר 5.8.2026. תדריכים הולכים לשם, לא לשדה `אינטראקציה` של איש הקשר, ששמור ליומן ההתכתבות עצמה.

## איך מגיעים לתוכן השיחה

### אינסטגרם — דרך כרום, וזה עובד

הדרך המעשית היחידה, ומאומתת בשטח:

1. `mcp__claude-in-chrome` → `navigate` ל-`instagram.com/direct/inbox/`. הילה מחוברת שם בכרום שלה.
2. **לחפש לפי שם המשתמש, לא לפי השם המוצג.** חיפוש לפי שם לא מצא שרשור קיים; `guytash` מצא אותו מיד.
3. `get_page_text` על השרשור — מחזיר את ההתכתבות המלאה עם חותמות זמן.

זה מחזיר הרבה יותר ממה שמגיע בהעתקה ידנית. בשרשור אחד שכבר הועתק לצ'אט, המשיכה מכרום חשפה נתונים שלא היו בהעתקה — היקף פניות חודשי, מערכת קיימת, ותשובות לשאלות סינון שלמות.

**לסגור את הכרטיסייה כשמסיימים.**

### וואטסאפ — צילום מסך, או אותו מסלול כרום

צילום מסך שהילה שולחת לצ'אט עובד ומספיק. WhatsApp Web בכרום הוא אותו מסלול כמו אינסטגרם וסביר שיעבוד באותה צורה — טרם נוסה.

### ManyChat — לא יעזור לקריאת שיחות

**ל-ManyChat API אין נקודת קצה שמחזירה היסטוריית שיחה.** זה חסר ידוע במוצר, עם בקשות פיצ'ר פתוחות בקהילה שלהם. ה-API נותן פרטי מנוי, תגיות, שדות מותאמים ושליחת הודעות — לא תמלול. לקרוא בממשק אפשר; להוציא תכנותית לא.

מה שכן אפשר: שלב **External Request** ב-Flow Builder שדוחף כל הודעה החוצה בזמן אמת ל-Make (שמחובר) ומשם ל-CRM. זה תופס שיחות **מכאן והלאה**, לא רטרואקטיבית.

### Meta API — הדרך היחידה לאוטומציה מלאה

הדרך היחידה שבה ליד ייכנס ל-CRM בלי מגע יד. דורשת חשבון אינסטגרם Professional מקושר לעמוד פייסבוק, אפליקציה ב-Meta, ואישור הרשאת `instagram_manage_messages`. הקמה אמיתית, לא הגדרה של חמש דקות. טרם נעשתה.

## מחקר לפני כתיבה — ומה לא ממציאים

לפני שקושרים חברה לליד, **לאמת מול מזהה קשיח**, לא מול התאמת שם:

- **מקרה שעבד:** מספר הוואטסאפ של הליד הופיע כמספר הרשמי באתר החברה. זיהוי ודאי.
- **מקרה שנפסל:** חיפוש ברשת החזיר שם חברה לליד אחר, אבל אף אחד מהקישורים שחזרו בפועל לא תמך בו. לא נכנס ל-CRM.
- **מקרה שהוכרע בשאלה:** שם החברה והאתר פשוט לא היו בשום מקום — לא בשרשור, לא בפרופיל, לא במייל. הפתרון היה לשאול את הילה, לא לנחש.

**שדה שאין לו ערך אמיתי נשאר ריק, ומדווח בצ'אט שהוא ריק ולמה.** שם, טלפון, תקציב, שם חברה — לא ממציאים ולא משלימים מהקשר. ערך שנגזר בהסקה (למשל ותק עסק שנלמד מ"12 שנה על אותה מערכת") מסומן ככזה בדיווח.

## מוזרויות של Airtable API ששווה לדעת מראש

**אי אפשר להוסיף אופציה ל-singleSelect דרך עדכון שדה.** ה-endpoint של `PATCH .../fields/<id>` מקבל רק `name` ו-`description`; שליחת `options` מחזירה 422. הדרך לעקוף: לכתוב את הרשומה עם `typecast: true` — ערך שלא קיים ברשימה נוצר כאופציה חדשה. כך נוספו `instagram` ו-`whatsapp` לשדה `מקור הגעה`.

זה במכוון **הפוך** ממה שסקיל `airtable-write` עושה בבייס התוכן, שם `typecast` מושבת בכוונה כדי שערך לא חוקי ייכשל ברעש. בטבלת מקורות ההגעה של ה-CRM ההתנהגות הרצויה היא ליצור את האופציה החסרה, לא ליפול.

**יצירת שדה חדש דורשת `schema.bases:write`** — הרשאה שנוספה ל-PAT ב-5.8.2026. בלעדיה: 403.
