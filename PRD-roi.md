# PRD — רועי (Roi), סוכן המנכ"ל

| | |
|---|---|
| **סטטוס** | טיוטה (Draft) |
| **תאריך** | 2026-07-05 |
| **Slug / שם קובץ** | `roi` |
| **בעלים** | הילה |

---

## 1. רקע ומטרה

**CreativeAgent** הוא סוויטת תוכן ויצירה מרובת-סוכנים (ראו [CLAUDE.md](CLAUDE.md)) בניהול **רועי (Roi)**. גפן היא בעלת הבית של אסטרטגיית ותוכן Instagram; נגה אחראית לכתיבה בערוצים האחרים כברירת מחדל; יעל מחזיקה אסטרטגיה עסקית ותכנון רב-ערוצי; ומירב מבצעת עיצוב לפי בריף תוכן מאושר.

> **הערה היסטורית:** בתחילת הפרויקט תוכננו במקום זאת **יובל** (יצירת קריאייטיב לפרסום) ו**יעל** (במובן של כותבת תוכן שיווקי שמפעילה את יובל). שני אלה **נמחקו לגמרי** על פי בקשת המשתמשת — הם לא היו חלק מהחזון ארוך-הטווח שלה. השם "יעל" נבנה כעת מחדש לתפקיד שונה לגמרי (יועצת אסטרטגית, ראו סעיף 6) — זהו צירוף מקרים בשם בלבד, לא המשך של המימוש הישן.

**רועי (Roi)** הוא הסוכן הראשי (המנכ"ל) של כל הצוות. רועי אינו מבצע עבודה בעצמו — תפקידו היחיד הוא **לנתב, להפעיל ולנהל** את העובדים תחתיו בהתאם למשימה שמתקבלת מהמשתמש, ולהחזיר למשתמש דוח מאוחד.

**דרישת מפתח (בוצעה):** `CLAUDE.md` מגדיר שרועי הוא **נקודת הכניסה החובה לכל בקשה** — כל פנייה של המשתמש לפרויקט עוברת דרכו קודם כל, ללא יוצא מן הכלל.

---

## 2. מטרות (Goals)

1. לספק שכבת ניהול (orchestration) אחת שדרכה עוברת כל בקשה של המשתמש.
2. לנתב כל משימה לעובד/ים המתאימים ביותר מתוך עובדי הצוות (7 מתוכננים, 4 בנויים כרגע), על בסיס כללים מוגדרים מראש.
3. לאפשר הפעלה מקבילה של מספר עובדים כאשר משימה חוצה תחומי אחריות.
4. לרכז את כל התוצאות שהתקבלו מהעובדים לכדי דוח/תשובה אחת, ברורה ומאוחדת, למשתמש.
5. לשמור על הפרדת אחריות מוחלטת: רועי מנהל ומחליט — הוא **לא** מבצע את העבודה בעצמו, אף לא במשימות טריוויאליות.

## 3. מחוץ לסקופ (Out of Scope) — לגרסה זו

- הגדרת זהות, תפקיד ותחום האחריות המדויק של העובד המתוכנן שעדיין לא נבנה (בר).
- כתיבת `SKILL.md` לרועי (אם וכאשר יידרש סקיל תומך) — ייבחן בנפרד.
- הגדרת `agent.md` / `SKILL.md` לעובד המתוכנן הנותר.

---

## 4. משתמשים ותרחיש שימוש מרכזי

**משתמשת:** הילה, מפעילה את הצוות דרך Claude Code.

**תרחיש בסיסי:**
1. הילה כותבת בקשה/בריף (בעברית או אנגלית) — לדוגמה דרך `/roi <בקשה>` או ישירות בשיחה.
2. רועי קורא את הבקשה, מזהה את תחום/י העיסוק הרלוונטיים.
3. רועי בודק את טבלת הניתוב (סעיף 6) ומחליט איזה עובד/ים להפעיל.
4. אם יש התאמה ברורה — רועי מפעיל את העובד/ים (אחד או יותר, במקביל אם נדרש).
5. אם אין התאמה ברורה — רועי **עוצר**, ומדווח למשתמשת שהבקשה עמומה, ומציג את המועמדים הקרובים ביותר לבחירה.
6. כאשר העובד/ים מסיימים, רועי מאחד את כל התוצאות (טקסט, קבצים, נתיבי output) לכדי **דוח אחד מסודר**.
7. רועי מחזיר למשתמשת: מה הוא הבין מהבקשה, למי הוא פנה ולמה, מה כל עובד הפיק (כולל נתיבי קבצים), וסיכום מאוחד.

---

## 5. עקרונות יסוד (Non-Negotiable Behaviors)

| # | עיקרון |
|---|--------|
| 1 | רועי הוא נקודת הכניסה היחידה — כל בקשה עוברת דרכו. |
| 2 | רועי **תמיד מאציל** לעובד מתאים. הוא לעולם לא מבצע את העבודה בעצמו, גם לא במשימות פשוטות/טריוויאליות. |
| 3 | הניתוב מבוסס **כללים מוגדרים מראש** (routing table), לא "ניחוש" חופשי של המודל. |
| 4 | מותר ואף רצוי להפעיל **מספר עובדים במקביל** כאשר המשימה חוצת-תחומים. |
| 5 | כאשר אין התאמה ברורה לאף כלל — רועי **לא מנחש**. הוא עוצר ומדווח למשתמש, ומבקש הבהרה. |
| 6 | כאשר מופעלים מספר עובדים, רועי **מאחד** את כל הפלטים לכדי סיכום אחד — לא מחזיר תוצאות גולמיות בנפרד ללא עיבוד. |
| 7 | רועי תמיד מתעד ומחזיר "שקיפות החלטה" — למה בחר בעובד הזה ולא באחר. |

---

## 6. העובדים (Employees)

הצוות תחת רועי מתוכנן ל-**7 עובדים בסך הכל**. **6 מוגדרים ופעילים כיום**: `liat`, `noga`, `merav`, `dani`, `yael`, `gefen`. **1 מתוכנן אך טרם נבנה** (יש לו שם ותפקיד מוגדרים, רק לא agent.md): `bar` (משווק בלינקדאין). מבנה הניהול נשאר **generic**: הוספה/שינוי של עובד לא דורש שינוי בליבת ההיגיון של רועי — רק עדכון טבלת הניתוב.

### טבלת ניתוב (Routing Table)

| # | Slug עובד | תחום אחריות | תנאי הפעלה / מילות מפתח בבריף | תלות בעובדים אחרים |
|---|-----------|--------------|-------------------------------|----------------------|
| 1 | `liat` | מחקר טרנדים ומאמרים בנושאי שיווק ואוטומציות, מחקרי עומק | חפש, מצא, מחקר, מגמות, מאמר על, חדש על, מה קורה עם, מקור על, שיווק, אוטומציות / search, find, research, trends, article about, latest on, news on, marketing, automation | ללא (מדווחת לרועי בלבד, לא מפעילה עובדים אחרים) |
| 2 | `noga` | כתיבת תוכן רב-ערוצי: ניוזלטרים, בלוג, לינקדאין, פייסבוק וטיקטוק. Instagram רק בהפניה מפורשת של הילה | כתיבה ועריכה בערוצים אלה; Instagram אינו trigger עבורה כברירת מחדל | ללא תלות בגפן |
| 3 | `merav` | יצירת תמונות ועיצוב חזותי (`gpt-image-2` ו-`brand-renderer`) | בקשת עיצוב או בריף חזותי מאושר | ב-Instagram מקבלת את הבריף מגפן; בערוצים אחרים לפי הזרימה הרלוונטית |
| 4 | `yael` | אסטרטגיה עסקית, מוצרים, קהלים, הצעות, השקות ותכנון רב-ערוצי. אינה gateway קבוע ל-Instagram | בקשות אסטרטגיה עסקית או תכנון חוצה ערוצים | גפן מקבלת ממנה הקשר רק כשנדרש |
| 5 | `gefen` | בעלת הבית של אסטרטגיית ותוכן Instagram: אבחון, תוכניות, נושאי-אב, Hooks, Reels, Carousels, Captions, Stories, CTA, תסריטים, Reference Analysis, בריפים למירב ולמידה מביצועים | כל בקשת Instagram רגילה, אבחון, Insights, Reference או גפן | ניתוב ישיר; רועי נכנס רק כשנדרש workflow חוצה סוכנים או מערכות |
| Ops | `instagram-ops` | תמיכה תפעולית ומדידה ל-Instagram: מצב עובדתי, קישורים, חסמים, Evidence ודוחות לגפן | סטטוס Instagram, איסוף Insights, performance report, עדכון current state | אינו חלק מה-roster הקריאייטיבי; אינו כותב או מפרש תוכן |
| 6 | `bar` | שיווק בלינקדאין *(טרם נבנה)* | `TBD` | `TBD` |
| 7 | `dani` | SEO: הנחיות מקדימות ל-`noga` (מילות מפתח, מבנה כותרות מוצע) לפני כתיבה, וסקירה + חבילת מטא-דאטה סופית לפרסום (כותרת SEO, מטא תיאור, slug) אחרי כתיבה — רק לתוכן בלוג/אתר או לינקדאין | SEO, קידום אתרים, מילות מפתח, מטא תיאור, כותרת SEO, אופטימיזציה לגוגל, דירוג בגוגל / seo, keyword research, meta description, title tag, search ranking, optimize for search | מופעל ע"י רועי פעמיים סביב `noga`: לפני (בריף SEO מקדים) ואחרי (סקירה + מטא-דאטה) — רק כשהיעד בלוג/אתר או לינקדאין |

> `bar` עדיין ללא `agent.md` ו-trigger keywords — יעודכן כשיוגדר, בדומה לתהליך שבוצע לליאת, נגה, מירב, דני, יעל וגפן. עד אז, אם בקשה נראית מתאימה לו — רועי עוצר ומדווח למשתמשת שהעובד עדיין לא מוגדר, במקום להמציא אותו.

---

## 7. לוגיקת ניתוב (Routing Logic)

1. רועי מקבל בריף/בקשה.
2. הוא בודק את טבלת הניתוב (סעיף 6) לפי מילות מפתח/תנאים.
3. **התאמה יחידה וברורה** → מפעיל את אותו עובד בלבד.
4. **מספר התאמות** (המשימה חוצה תחומים) → מפעיל את כל העובדים הרלוונטיים **במקביל**, ומעביר לכל אחד את חלק הבריף הרלוונטי אליו.
5. **אין התאמה / הבריף עמום** → **לא מפעיל אף עובד**. עוצר, ומציג למשתמשת:
   - מה בבריף יצר את חוסר הבהירות.
   - אילו עובדים הם "מועמדים קרובים" ולמה.
   - שאלה ממוקדת שתאפשר להמשיך.

---

## 8. הפעלה מקבילה (Parallel Dispatch)

- כאשר משימה דורשת מספר עובדים — רועי מפעיל אותם **במקביל**, לא בסדרה, אלא אם יש תלות מפורשת בין העובדים (למשל עובד ב' זקוק לפלט של עובד א' — במקרה כזה ההפעלה תהיה סדרתית לפי התלות המוגדרת בטבלת הניתוב, עמודה "תלות בעובדים אחרים").
- לכל עובד מועבר רק החלק הרלוונטי בבריף + הקשר נדרש (למשל קבצי הפניה, נתיבי output קודמים).

## 9. איחוד תוצאות (Result Aggregation)

בתום ההפעלה, רועי בונה דוח יחיד למשתמשת בפורמט הבא:

```
## מה הבנתי מהבקשה
<תמצית הבריף כפי שרועי הבין אותו>

## למי פניתי ולמה
- <עובד 1>: <סיבת הבחירה>
- <עובד 2>: <סיבת הבחירה> (אם רלוונטי)

## תוצאות
### <עובד 1>
<פלט / נתיב קובץ / החלטות עיצוב עיקריות>

### <עובד 2>
<פלט / נתיב קובץ>

## סיכום מאוחד
<איך התוצרים מתחברים זה לזה, מה המשתמשת מקבלת בפועל, המלצות/צעדים הבאים אם יש>
```

---

## 10. מבנה קבצים נדרש

| נתיב | תפקיד |
|------|-------|
| `PRD-roi.md` | מסמך זה |
| `.claude/agents/roi.md` | פרסונת סוכן רועי — הליבה של ההיגיון (ניתוב, הפעלה מקבילה, איחוד תוצאות, fallback) |
| `.claude/commands/roi.md` | *(עתידי, לא בסקופ זה)* פקודת `/roi` שמעבירה `$ARGUMENTS` לסוכן |
| `.claude/agents/liat.md` | פרסונת סוכנת ליאת — מחקר טרנדים בשיווק/אוטומציות (מוגדרת ופעילה) |
| `.claude/agents/noga.md` | פרסונת סוכנת נגה — כותבת התוכן (מוגדרת ופעילה, scope מורחב) |
| `.claude/agents/merav.md` | פרסונת סוכנת מירב — יצירת תמונות תוכן (מוגדרת ופעילה) |
| `.claude/skills/gpt-image-gen/SKILL.md` | סקיל עוטף לקריאת OpenAI Images API (`gpt-image-2`) — משמש את מירב |
| `.claude/agents/dani.md` | פרסונת סוכן דני — איש ה-SEO (מוגדרת ופעילה): הנחיות מקדימות לנגה + סקירה וחבילת מטא-דאטה סופית |
| `dani/outputs/` | חבילות ה-SEO של דני (`<name>-seo.md`), תואמות לשמות קבצי הפלט של נגה |
| `.claude/agents/yael.md` | פרסונת סוכנת יעל — היועצת האסטרטגית (מוגדרת ופעילה): תוכניות תוכן חודשיות + שער אסטרטגי לנושאי תוכן חדשים |
| `.claude/skills/airtable-read/SKILL.md` | סקיל read-only לקריאת ה-REST API של Airtable — משמש את יעל לבדיקת טבלאות התכנים/תכנון; אין בו קריאות כתיבה/עדכון/מחיקה |
| `.claude/skills/docx-export/SKILL.md` | סקיל להמרת קובץ `.md` עברי (RTL) ל-`.docx` תואם (טבלאות/כותרות אמיתיות, יישור ימין) דרך `python-docx` — משמש את יעל להפקת עותק Word לצד כל `.md` שהיא שומרת |
| `.claude/skills/email-send/SKILL.md` | סקיל שליחה-בלבד (curl ל-Resend API) — משמש את הג'וב המתוזמן השבועי לשליחת תצוגה מקדימה של תוכנית השבוע הבא להילה; אין בו יכולת קריאת תיבת דואר נכנס/תשובות |
| `yael/strategy.md` | מסמך האסטרטגיה והתפעול של יעל (הועתק/תומלל מ-`מסמך_אסטרטגיה_עבור_יעל.docx`) — רקע, חזון, שני מסלולי הכנסה, קהל יעד, כללי תוכן ומיתוג |
| `yael/outputs/` | תוכניות התוכן החודשיות של יעל, כזוג קבצים תואם (`<YYYY-MM>-content-plan.md`/`.docx`) |
| `.claude/agents/gefen.md` | Gefen 2.0 — בעלת הבית של אסטרטגיית ותוכן Instagram, כולל Reference Analysis ולמידה מביצועים |
| `.claude/agents/instagram-ops.md` | Instagram Ops — סוכן תמיכה תפעולי ומדידה צר; Facts בלבד, ללא פרשנות קריאייטיבית |
| `gefen/outputs/` | קובצי המלצות מבנה הפרופיל של גפן (`<YYYY-MM-DD>-<slug>-profile-structure.md`) — הבריף/הסקירה הפר-פוסט שלה לא נשמרים לקובץ |
| `.claude/agents/<slug>.md` | *(עתידי)* פרסונת `bar`, כשיוגדר במלואו |
| `CLAUDE.md` | עודכן — מגדיר שרועי הוא הכניסה החובה לכל בקשה, כולל טבלת ניתוב וזרימת "תוכן חדש מהאינטרנט" |

---

## 11. קריטריוני הצלחה / קבלה (Acceptance Criteria)

- [ ] `.claude/agents/roi.md` קיים, עם frontmatter תקין וזיהוי עצמי כ"מנכ"ל/מנתב בלבד".
- [ ] רועי אינו מבצע אף פעם עבודה בעצמו — בכל תרחיש בדיקה הוא מאציל לעובד.
- [ ] כאשר יש התאמה עמומה/חסרת כלל — רועי עוצר ושואל, ולא מנחש.
- [ ] כאשר יש 2+ עובדים רלוונטיים — רועי מפעיל במקביל ומחזיר סיכום מאוחד אחד, לא פלטים גולמיים נפרדים.
- [ ] דוח התשובה תמיד כולל: הבנת הבקשה, בחירת העובד/ים ונימוק, תוצרים, וסיכום מאוחד.

---

## 12. שאלות פתוחות / עבודה עתידית

1. ~~הגדרת זהות, תחום אחריות ופרומפט מפורט לעובדים~~ — **חלקית**: `liat`, `noga`, `merav`, `dani`, `yael`, `gefen` מוגדרים ופעילים בטבלת סעיף 6; `bar` עדיין TBD.
2. האם רועי/נגה/מירב זקוקים ל־`SKILL.md` תומכים? להערכה בהמשך.
3. יצירת `.claude/commands/roi.md` לפקודת `/roi`.
4. ~~עדכון `CLAUDE.md` כך שרועי יוגדר כנקודת כניסה חובה לכל בקשה בפרויקט~~ — **בוצע**.
5. ~~הגדרת `dani` (איש SEO) במלואו~~ — **בוצע**. ~~הגדרת `yael` (יועצת אסטרטגית) במלואה~~ — **בוצע**. ~~הגדרת `gefen` (משווקת אינסטגרם) במלואה~~ — **בוצע** (2026-07-10). נותר: הגדרת `bar` במלואו (agent.md, trigger keywords) והשלמת טבלת הניתוב.
6. `noga/style-guide.md` עדיין לא נכתב (המשתמשת ציינה שתיצור אותו בנפרד) — נגה כרגע פועלת עם ברירת מחדל וכותבת בדוח שלה שלא נמצא מדריך סגנון.
7. ~~מחיקת יובל ויעל (המימוש המקורי)~~ — **בוצע**.
8. ~~מנוע התמונות של מירב~~ — **שונה שוב**: הוחלף ממנוע Gemini/Nano Banana 2 מבוסס-Node (`scripts/generate.mjs`) לגישה מבוססת OpenAI `gpt-image-2` דרך סקיל `gpt-image-gen` (curl/Bash). `scripts/generate.mjs` ו-`package.json` נמחקו — אין יותר תלות ב-Node/npm בפרויקט. `GEMINI_API_KEY` הוחלף ב-`OPENAI_API_KEY` ב-`.env`/`.env.example`.

---

## 13. יומן החלטות (Decision Log)

| החלטה | תשובה שנבחרה |
|-------|----------------|
| תפקידי הצוות | 7 עובדים מתוכננים; `liat`, `noga`, `merav`, `dani`, `yael`, `gefen` בנויים ופעילים; `bar` עדיין TBD |
| תפקיד דני (SEO) | הנחיות SEO מקדימות לנגה לפני כתיבה (מילות מפתח/מבנה כותרות, עם גישת אינטרנט לחיפוש חי) + סקירה חד-פעמית אחרי כתיבה עם חבילת מטא-דאטה (כותרת SEO/מטא תיאור/slug/alt-text) — לא לולאת תיקון אוטומטית; פועל רק על תוכן לבלוג/אתר או לינקדאין (לא פייסבוק/אינסטגרם/טיקטוק) |
| dani/ בגיט | לא — מוגדר ב-.gitignore, כמו `noga/`, `liat/`, `merav/` |
| תפקיד יעל (יועצת אסטרטגית) | שני מצבים: (1) בניית תוכניות תוכן חודשיות (סושיאל/ניוזלטר/אתר) לפי `yael/strategy.md` (שירותים/מסעות לקוח/חזון/יעדים), נשמר כזוג `yael/outputs/<YYYY-MM>-content-plan.md`+`.docx`; (2) שער אסטרטגי — מאשרת **נושא** תוכן חדש לפני הפקה (לא סוקרת טיוטה מוגמרת, זה לא בתחומה). ללא גישת אינטרנט כללית — עובדת מהמסמכים הפנימיים, מתוצרי הצוות הקיימים, ומקריאה מוגבלת (read-only) לטבלאות התכנים באיירטייבל |
| פורמט פלט של יעל | כל תוכנית תוכן נשמרת כזוג קבצים תואם — `.md` (המקור) + `.docx` (מיוצא דרך סקיל `docx-export`, לא אופציונלי) — כמו הצמד `.md`+`.html` של נגה. הוחלט (2026-07-09) אחרי שהמשתמשת ציינה שקשה לה לקרוא md. הסקיל מבוסס `python-docx` ולא Node/npm (לא זמין בסביבה) |
| `yael/strategy.md` | נכתב בפועל (2026-07-09) — תומלל מ-`מסמך_אסטרטגיה_עבור_יעל.docx` שהעבירה המשתמשת. כולל: רקע/בידול, חזון (מעבר מבנק שעות למוצרים דיגיטליים בסקייל), שני מסלולי הכנסה + טבלת מוצרים, תהליך בניית מוצר, קהל יעד, כללי תוכן/מיתוג, ואיך יעל מנחה את רועי |
| גישת יעל לאיירטייבל | קריאה בלבד (read-only) **מכל הטבלאות הרלוונטיות** בבייס האיירטייבל של המשתמשת (11 טבלאות בבייס `appIgWywUrIU1jcHy`, כולל מאגר תוכן קיים, תכנון תוכן חודשי, תוכנית כלכלית חדש ועוד — הרשימה המלאה + Table IDs ב-`.claude/skills/airtable-read/SKILL.md`) — דרך סקיל ייעודי `airtable-read` (REST API + `AIRTABLE_API_KEY`/`AIRTABLE_BASE_ID` ב-`.env`), לא MCP connector (לא היה זמין בסביבה — אין קונקטורים מותקנים כלל, ואיירטייבל לא מופיע ברשימת הקונקטורים הניתנים להוספה). ל-`yael` ניתן כלי `Bash` המוגבל לשימוש בסקיל הזה בלבד — אין קריאות כתיבה/עדכון/מחיקה. הפיכת טבלה ל"ציבורית" באיירטייבל (share link) **אינה** מספקת גישת API — עדיין נדרש Personal Access Token בכל מקרה |
| yael/ בגיט | לא — מוגדר ב-.gitignore, כמו `noga/`, `liat/`, `merav/`, `dani/` |
| תפקיד Gefen 2.0 (2026-08-19) | בעלת הבית של Strategy + Content ב-Instagram. מקבלת בקשות Instagram ישירות, כותבת את התוצרים, מוסרת בריפים חזותיים למירב ומפרשת Evidence עובדתי שמספק Instagram Ops. אינה מנהלת את Google Sheet התפעולי |
| הפעלת גפן — יעל ממליצה, רועי מפעיל | יעל **לא** קיבלה כלי `Agent` ואינה מפעילה את גפן ישירות — זה היה שובר את העיקרון הארכיטקטוני שרק רועי מפעיל עובדים. במקום זאת, יעל יכולה להמליץ על מעורבות גפן בעמודת "עובדים מומלצים ובאיזה סדר" בתוכניות/בפסיקות שלה, ורועי הוא זה שקורא את ההמלצה ומפעיל את גפן בפועל אם רלוונטי |
| gefen/ בגיט | לא — מוגדר ב-.gitignore, כמו `noga/`, `liat/`, `merav/`, `dani/`, `yael/` |
| יובל ויעל (מימוש מקורי) | נמחקו לגמרי לפי בקשת המשתמשת |
| מנוע התמונות של מירב | OpenAI `gpt-image-2` דרך סקיל `gpt-image-gen` (curl/Bash) — לא Node/Gemini; `package.json`/`scripts/generate.mjs` נמחקו |
| תיקיות עבודה של מירב | `merav/reference/` (סגנון) ו-`merav/outputs/` (תוצרים + prompt נלווה), לא בגיט — לא עוד `references/`/`output/` המשותפים |
| מייל תוכנית שבועית + שער אישור (2026-07-09) | ג'וב מתוזמן חוזר (`weekly-content-plan-email`, cron `0 7 * * 4`, כל יום חמישי ~7:00) — קורא את תוכנית `yael` הנוכחית, מבקש מרועי תמצית עברית של השבוע הבא (preview בלבד, **בלי** להפעיל `liat`/`dani`/`noga`/`merav`), ושולח אותה במייל דרך `email-send`. **אין** יכולת ניתוח תשובות מייל — האישור תמיד חוזר בצ'אט בשיחה עתידית, וזה הטריגר להפעלת `yael`+הצוות בפועל |
| הרשאות רועי לא הורחבו | בקשה ראשונית הייתה לתת ל-`roi` `Bash` כדי לשלוח את המייל בעצמו — נחסם ע"י בדיקת בטיחות (הרחבת הרשאה שלא התבקשה במפורש). הפתרון: רועי מכין תמצית טקסט בלבד (בתוך יכולותיו הקיימות), ושליחת המייל בפועל מתבצעת ע"י שכבת התזמון שמפעילה אותו, לא ע"י הפרסונה של רועי עצמה — `roi.md` נשאר `Agent, Read, Glob, Grep` בלבד |
| מנגנון התזמון | נעשה שימוש בכלי ה-cloud-scheduled-tasks (durable, שורד בין הרצות/סשנים) ולא ב-`CronCreate` (session-only, נמחק אחרי 7 ימים/סגירת הסשן) — לא מתאים לאוטומציה קבועה כמו זו |
| חיבור נגה↔מירב | נגה משאירה placeholders `{{IMAGE_NEEDED: "..."}}` בטקסט; רועי מפעיל את מירב לפיהם ומשלב את התמונות בקבצים הסופיים |
| נגה — טבלאות/גרפים | טבלאות Markdown/HTML בלבד; אין יכולת גרפים אמיתית (אין לה הרצת קוד) |
| noga/ בגיט | לא — מוגדר ב-.gitignore, כמו references/writing/ שהיה קודם (עקבי עם ההחלטה על Content/ ו-liat/Memory/) |
| לוגיקת ניתוב | כללים מוגדרים מראש (routing table) |
| הפעלה מקבילה | מותרת, כשנדרש |
| טיפול במקרה עמום/ללא התאמה | רועי עוצר ומדווח למשתמשת |
| איחוד תוצאות ריבוי-עובדים | סיכום מאוחד אחד |
| ביצוע עצמי ע"י רועי | אף פעם לא — תמיד מאציל |
| Slug | `roi` |
| Content/ ו-liat/Memory/ בגיט | לא — מוגדרים ב-.gitignore, כמו output/ ו-references/ |
