# אתר הקורסים — מסמך התמצאות

אתר הקורסים **אינו** חלק מהתיקייה הזו. הוא פרויקט Lovable נפרד. הקובץ הזה קיים כדי שלא יהיה צורך לשאול את הילה שוב איפה דברים נמצאים — הוא לא מתעד את המערכת, הוא מפנה לתיעוד שלה.

## זהות הפרויקט

| | |
|---|---|
| שם ב-Lovable | `gananicourses` ("Course Connect Hub") |
| Project ID | `28be939f-66fe-4dbb-8b88-fbebbaa74151` |
| Workspace | `SDhpfFncRNZhwHGbc7jS` ("Hila's Lovable") |
| כתובת בפרודקשן | `https://courses.ganani.co.il` |
| כתובת Lovable | `https://gananicourses.lovable.app` |
| Stack | TanStack Start (TypeScript) + Supabase |

גישה לקוד מהשיחה הזו: דרך ה-MCP של Lovable (`read_file` / `list_files` עם ה-Project ID למעלה). אין עותק מקומי.

## סליקה: SUMIT

**ספק הסליקה הוא SUMIT.** התזרים המלא, כולל הבאג ההיסטורי שתוקן והארכיטקטורה הנוכחית, מתועד ב-`docs/purchase-flow.md` **בתוך פרויקט הקורסים** — זה מקור האמת, לא הקובץ הזה.

תמצית, לצורך התמצאות בלבד:

1. SUMIT מפנה את הקונה ל-`/api/public/sumit/return`
2. הנקודה קוראת ל-`finalizeOrder` ב-`src/lib/sumit.server.ts`
3. `finalizeOrder` מאמת את התשלום מול SUMIT server-to-server
4. אם אומת, רץ רצף קבוע: `ensureOrderUser` (יצירת משתמש) → `enrollPaidOrder` (הרשמה לקורס) → `sendWelcomeEmailIfNeeded` (מייל ברוכים הבאים דרך Resend)

יצירת הגישה קורית **בצד השרת**, לא בדפדפן — זה תוקן במכוון אחרי שקונים נשארו בלי גישה כשהדפדפן נסגר. אין לחזור לתלות בצד הלקוח.

קבצים מרכזיים: `src/lib/sumit.server.ts` · `src/lib/payments.functions.ts` · `src/lib/welcome-email.server.ts` · `src/routes/checkout.tsx`

## נקודות חיבור לאוטומציה

טבלת `orders` ב-Supabase היא המקום הנכון להאזין לרכישות. **עדיף על נגיעה בקוד תזרים התשלום**, שנבדק end-to-end ואין סיבה לסכן אותו.

**קיים כבר (28.7.2026):** הטריגר `trg_notify_make_order_paid` על `public.orders` שולח webhook ל-Make בכל מעבר `pending → paid`, ומשם הרוכש נכנס לרשימת "רוכשים 7 יסודות" בסמוב. הפירוט המלא — כולל המזהים, מה נבדק ומה לא, והמגבלה שאין בו סינון לפי קורס — ב-[`launch-7-yesodot.md`](launch-7-yesodot.md) §7. **אוטומציה נוספת על רכישות צריכה להתחבר לאותו טריגר, לא ליצור מנגנון מקביל.**

הערה טכנית: `pg_net` הותקן ב-28.7.2026 לצורך הטריגר הזה (`net.http_post`). קודם לכן לא היה מותקן.

## 🔴 האתר כולו ב-noindex — ושני מנגנונים חוסמים, לא אחד

הילה מתכננת "לפתוח את האינדוקס יום-יומיים לפני ההשקה" (סביב 17–18.8). **הדגל בבסיס הנתונים לבדו לא יעשה את זה**, וזה ייכשל בשקט.

יש **שתי** חסימות נפרדות:

| מנגנון | היכן | היקף |
|---|---|---|
| `{ name: "robots", content: "noindex, nofollow" }` | `src/routes/__root.tsx`, בתוך `head()` של ה-root | **כל האתר, כל עמוד** |
| `courses.noindex` בבסיס הנתונים | נקרא ב-`src/routes/course.$slug.tsx` | דף קורס בודד |

**אומת על האתר החי 29.7.2026:** קורס שמסומן `noindex: false` בבסיס הנתונים (`meta-leads`) **עדיין מחזיר `noindex, nofollow`** — כי המטא של ה-root חל עליו. גם דף הבית. כלומר כיבוי הדגל בבסיס הנתונים לא משנה דבר כל עוד השורה ב-`__root.tsx` קיימת.

**מה שצריך לקרות בפועל כדי לפתוח לאינדוקס:** להסיר/להתנות את שורת ה-`robots` ב-`__root.tsx` **וגם** לכבות `noindex` על הקורס. שני הצעדים, לא אחד.

`robots.txt` דווקא מתיר סריקה של דפי הקורסים — החסימה כולה ברמת תג המטא.

`7-marketing-foundations` ו-`manychat` מסומנים `noindex: true` בבסיס הנתונים; ארבעת הקורסים בסטטוס `soon` מסומנים `false` (ובכל זאת חסומים, מהסיבה שלמעלה).

**שתי מסקנות מעשיות:**
- **תנועה אורגנית לא תתרום להשקה, ואין לצפות שתתרום.** גם אם הדגל ייכבה ב-18.8, אינדוקס לוקח ימים עד שבועות — ורצף המכירה רץ 20.8–10.9. ההשקה היא ערוץ מייל, נקודה.
- **`noindex` לא חוסם מדידה.** GA4 עובד מצוין על עמוד שאינו מאונדקס — השניים בלתי תלויים. אין סיבה טכנית לחכות עם התקנת אנליטיקס עד לפתיחת האינדוקס, ויש סיבה טובה שלא: התקנה אחרי 20.8 מפספסת את תחילת הרצף.

## ⚠️ אין אנליטיקס באתר הקורסים (נבדק 29.7.2026)

נבדק על האתר החי, לא מהקוד: **אין GA4, אין GTM, אין פיקסל של מטא, אין Clarity/Hotjar/Plausible — כלום.** בעמוד יש שני תגי `<script>` בסך הכל, שניהם של האפליקציה, ואפס בקשות רשת ליעדי מדידה. באנר העוגיות (`src/components/cookie-consent.tsx`) שומר בחירה ב-localStorage אבל **לא טוען שום סקריפט על בסיסה** — הוא ממתין למדידה שמעולם לא הותקנה.

**מה זה אומר בפועל:** פרמטרי UTM על קישורים לאתר הזה **נשמרים ב-URL** (נתיב `/course/$slug` הוא בלי `validateSearch`, ולכן הראוטר לא מפיל אותם) אבל **אף אחד לא קורא אותם**. הם טקסט בשורת הכתובת ותו לא. כל עוד המצב הזה נמשך, אי אפשר לייחס אף ביקור לקמפיין — כולל תשעת מיילי ההשקה.

התיוג עצמו נכון וכדאי להשאירו: ברגע שיותקן GA4 הוא יתחיל לעבוד רטרואקטיבית מרגע ההתקנה, בלי לגעת שוב בתוכן.

**נקודה שתהיה רלוונטית כשיותקן GA4, ולא לפני כן:** `handleBuy` ב-`src/routes/course.$slug.tsx` מנווט ל-`/checkout` עם `search: { course: slug }` שדורס את כל הפרמטרים, ו-`checkout.tsx` מריץ `validateSearch` עם סכמת zod שמסננת מפתחות לא מוכרים — כלומר ה-UTM נעלם במעבר לתשלום. **זה לא באג לתיקון**: GA4 מייחס מקור בתחילת סשן, ולכן הנתון כבר נתפס בכניסה לדף הנחיתה. לתעד כדי שלא "יתוקן" בטעות בעתיד.

## התקנת GA4 באתר הקורסים — מה כרוך בזה (נבדק 29.7.2026)

### מה כבר קיים באתר הראשי

`www.ganani.co.il` מריץ סטאק מדידה מלא ובוגר, שנקרא ישירות מהדף החי:

| כלי | מזהה |
|---|---|
| Google Tag Manager | `GTM-NBWG2MQ` |
| GA4 | `G-S7E1GGMBGD` |
| Google Ads | `AW-695737498` |
| Meta Pixel | מותקן (`fbq`) |
| Microsoft Clarity | `n8s9hdebtz` |
| LinkedIn Insight | מותקן |

### ההמלצה: זרם נתונים שני באותו נכס, לא נכס חדש

**לא ליצור GA4 property חדש לאתר הקורסים.** להוסיף אותו כ-**data stream נוסף בתוך `G-S7E1GGMBGD`**, ולהגדיר **מדידה חוצת-דומיינים** בין `www.ganani.co.il` ל-`courses.ganani.co.il`.

הסיבה מעשית: המשפך האמיתי עובר בין שני האתרים (בלוג → דף מכירה → רכישה, או מייל → דף מכירה). נכס נפרד היה מפצל כל מסע כזה לשני סשנים מנותקים ומייחס את הרכישה ל"referral מ-ganani.co.il" במקום למקור האמיתי. `courses.ganani.co.il` הוא תת-דומיין, כך שגם הקוקי משותף — זה התרחיש שחוצה-דומיינים נועד לו.

⚠️ **בלי הגדרת cross-domain, כל הקישורים מהאתר הראשי לקורסים ייספרו כ-self-referral** ויאפסו את הייחוס — זו התקלה הנפוצה ביותר בהתקנה כזו.

### היכן הקוד נכנס בפועל

`src/routes/__root.tsx` → `RootShell` — יש שם `<head>` עם `<HeadContent />` ו-`<body>` עם `<Scripts />`. זו נקודת ההזרקה היחידה שחלה על כל האתר. TanStack Start תומך בהוספת `scripts` ב-`head()` של ה-root.

**החלטה נדרשת: GTM או gtag ישיר.** GTM (`GTM-NBWG2MQ`) יאפשר לנהל את שאר הכלים (פיקסל, Clarity, Ads) מאותו מקום בלי לגעת בקוד שוב; gtag ישיר פשוט יותר אבל כל תוספת עתידית תדרוש שינוי קוד ב-Lovable, שעולה קרדיטים.

### באנר העוגיות — חייב להתחבר בפועל

`src/components/cookie-consent.tsx` שומר `accepted`/`rejected` ב-`localStorage` תחת `ganani_cookie_consent` — **ולא טוען שום סקריפט על בסיס הבחירה.** כרגע זה באנר ריק. ברגע שנכנס GA4, **הבאנר חייב לשלוט בו בפועל** (Google Consent Mode v2, או טעינה מותנית), אחרת הוא מבטיח למשתמש שליטה שאינה קיימת. הלוגיקה כבר כתובה — חסר רק החיבור.

### מה זה לא פותר

- **`noindex` אינו קשור.** GA4 עובד מצוין על עמוד שאינו מאונדקס — אין סיבה טכנית לחכות לפתיחת האינדוקס, ויש סיבה טובה שלא: התקנה אחרי 20.8 מפספסת את תחילת רצף ההשקה.
- **Search Console הוא נכס נפרד.** ה-property המוגדר בפרויקט הזה הוא `https://www.ganani.co.il/` בלבד. `courses.ganani.co.il` דורש הוספה נפרדת, ורלוונטי רק אחרי פתיחת האינדוקס.
- **המדידה של ההשקה עצמה לא תלויה בזה.** לפי §8 ב-`launch-7-yesodot.md` הבדיקה היא ספירת מכירות מטבלת `orders`. GA4 מוסיף ייחוס לערוץ, לא את המספר עצמו.

### עלות ביצוע

השינוי בקוד נעשה דרך הסוכן של Lovable (`send_message`) ו**צורך קרדיטים**. שווה לאגד אותו עם שינויים אחרים באתר — למשל דף האודות והקישור לאתר הראשי ([`courses-site-about-and-linking.md`](courses-site-about-and-linking.md)) — במקום שלוש בקשות נפרדות.

## עבודה פתוחה על האתר

[`courses-site-about-and-linking.md`](courses-site-about-and-linking.md) — בריף מוכן לשני דברים שנפתחו 29.7: **חיבור אתר הקורסים לאתר הראשי** (כרגע מנותקים לחלוטין בשני הכיוונים — הפוטר כותב `ganani.co.il` כטקסט ולא כקישור) ו**יצירת דף אודות** (לא קיים: אין route ואין שורה ב-`site_pages`). הבריף מחזיק את הדפוס המדויק ליצירת עמוד באתר הזה, ואת הרשימה הקצרה של מה שבאמת צריך להכריע מול הילה.

## מוצרים

| קורס | מחיר | כתובת |
|---|---|---|
| 7 יסודות שכל עסק צריך כדי לשווק את עצמו | 99 ₪ (מעוגן מול 279) · עולה ל-129 ב-11.9.2026 | `/course/7-marketing-foundations` |

9 שיעורי וידאו. **השיעור הראשון פתוח לצפייה חינם בדף המכירה, בלי הרשמה ובלי כרטיס אשראי.** ביטול תוך 14 יום כל עוד לא נצפו שיעורים.

## מה עוד יש ב-Lovable

- `one-on-one-meetings-hila-ganani` — דף פגישת ייעוץ (`b3705fe9-8e3d-457a-8fb0-7fe2fb661874`)
- `מסע הלקוח המוזהב` — תרשים זרימה, לא מפורסם (`c78ba64f-690e-4b05-af44-9713f34796cf`)
