# אתר הקורסים — מסמך התמצאות

אתר הקורסים **אינו** חלק מהתיקייה הזו. הוא פרויקט Lovable נפרד. הקובץ הזה קיים כדי שלא יהיה צורך לשאול את הילה שוב איפה דברים נמצאים — הוא לא מתעד את המערכת, הוא מפנה לתיעוד שלה.

## זהות הפרויקט

| | |
|---|---|
| שם ב-Lovable | `gananicourses` ("Course Connect Hub") |
| Project ID | `28be939f-66fe-4dbb-8b88-fbebbaa74151` |
| Workspace | `SDhpfFncRNZhwHGbc7jS` ("Hila's Lovable") |
| כתובת בפרודקשן | `https://courses.ganani.co.il` |
| כתובת Lovable | `https://gananicourses.lovable.app` |
| Stack | TanStack Start (TypeScript) + Supabase |

גישה לקוד מהשיחה הזו: דרך ה-MCP של Lovable (`read_file` / `list_files` עם ה-Project ID למעלה). אין עותק מקומי.

## סליקה: SUMIT

**ספק הסליקה הוא SUMIT.** התזרים המלא, כולל הבאג ההיסטורי שתוקן והארכיטקטורה הנוכחית, מתועד ב-`docs/purchase-flow.md` **בתוך פרויקט הקורסים** — זה מקור האמת, לא הקובץ הזה.

תמצית, לצורך התמצאות בלבד:

1. SUMIT מפנה את הקונה ל-`/api/public/sumit/return`
2. הנקודה קוראת ל-`finalizeOrder` ב-`src/lib/sumit.server.ts`
3. `finalizeOrder` מאמת את התשלום מול SUMIT server-to-server
4. אם אומת, רץ רצף קבוע: `ensureOrderUser` (יצירת משתמש) → `enrollPaidOrder` (הרשמה לקורס) → `sendWelcomeEmailIfNeeded` (מייל ברוכים הבאים דרך Resend)

יצירת הגישה קורית **בצד השרת**, לא בדפדפן — זה תוקן במכוון אחרי שקונים נשארו בלי גישה כשהדפדפן נסגר. אין לחזור לתלות בצד הלקוח.

קבצים מרכזיים: `src/lib/sumit.server.ts` · `src/lib/payments.functions.ts` · `src/lib/welcome-email.server.ts` · `src/routes/checkout.tsx`

## נקודות חיבור לאוטומציה

טבלת `orders` ב-Supabase היא המקום הנכון להאזין לרכישות. **עדיף על נגיעה בקוד תזרים התשלום**, שנבדק end-to-end ואין סיבה לסכן אותו.

**קיים כבר (28.7.2026):** הטריגר `trg_notify_make_order_paid` על `public.orders` שולח webhook ל-Make בכל מעבר `pending → paid`, ומשם הרוכש נכנס לרשימת "רוכשים 7 יסודות" בסמוב. הפירוט המלא — כולל המזהים, מה נבדק ומה לא, והמגבלה שאין בו סינון לפי קורס — ב-[`launch-7-yesodot.md`](launch-7-yesodot.md) §7. **אוטומציה נוספת על רכישות צריכה להתחבר לאותו טריגר, לא ליצור מנגנון מקביל.**

הערה טכנית: `pg_net` הותקן ב-28.7.2026 לצורך הטריגר הזה (`net.http_post`). קודם לכן לא היה מותקן.

## 🔴 האתר כולו ב-noindex — ושני מנגנונים חוסמים, לא אחד

הילה מתכננת "לפתוח את האינדוקס יום-יומיים לפני ההשקה" (סביב 17–18.8). **הדגל בבסיס הנתונים לבדו לא יעשה את זה**, וזה ייכשל בשקט.

יש **שתי** חסימות נפרדות:

| מנגנון | היכן | היקף |
|---|---|---|
| `{ name: "robots", content: "noindex, nofollow" }` | `src/routes/__root.tsx`, בתוך `head()` של ה-root | **כל האתר, כל עמוד** |
| `courses.noindex` בבסיס הנתונים | נקרא ב-`src/routes/course.$slug.tsx` | דף קורס בודד |

**אומת על האתר החי 29.7.2026:** קורס שמסומן `noindex: false` בבסיס הנתונים (`meta-leads`) **עדיין מחזיר `noindex, nofollow`** — כי המטא של ה-root חל עליו. גם דף הבית. כלומר כיבוי הדגל בבסיס הנתונים לא משנה דבר כל עוד השורה ב-`__root.tsx` קיימת.

**מה שצריך לקרות בפועל כדי לפתוח לאינדוקס:** להסיר/להתנות את שורת ה-`robots` ב-`__root.tsx` **וגם** לכבות `noindex` על הקורס. שני הצעדים, לא אחד.

`robots.txt` דווקא מתיר סריקה של דפי הקורסים — החסימה כולה ברמת תג המטא.

`7-marketing-foundations` ו-`manychat` מסומנים `noindex: true` בבסיס הנתונים; ארבעת הקורסים בסטטוס `soon` מסומנים `false` (ובכל זאת חסומים, מהסיבה שלמעלה).

**שתי מסקנות מעשיות:**
- **תנועה אורגנית לא תתרום להשקה, ואין לצפות שתתרום.** גם אם הדגל ייכבה ב-18.8, אינדוקס לוקח ימים עד שבועות — ורצף המכירה רץ 20.8–10.9. ההשקה היא ערוץ מייל, נקודה.
- **`noindex` לא חוסם מדידה.** GA4 עובד מצוין על עמוד שאינו מאונדקס — השניים בלתי תלויים. אין סיבה טכנית לחכות עם התקנת אנליטיקס עד לפתיחת האינדוקס, ויש סיבה טובה שלא: התקנה אחרי 20.8 מפספסת את תחילת הרצף.

## מדידה באתר הקורסים — GA4 הותקן 29.7.2026

**עד 29.7.2026 לא הייתה באתר שום מדידה** — לא GA4, לא GTM, לא פיקסל, לא Clarity. הבאנר שמר בחירה ב-localStorage בלי לטעון דבר. זה השתנה: **GA4 מותקן ופעיל**, בפירוט בסעיף הבא.

**מה עדיין לא מותקן, במכוון:** GTM, פיקסל של מטא, Clarity, LinkedIn Insight. כולם קיימים באתר הראשי ואף אחד מהם לא הועתק לכאן.

**המשמעות לתיוג קישורים:** עד ההתקנה, פרמטרי UTM על קישורים לאתר הזה נשמרו ב-URL אבל **אף אחד לא קרא אותם** — כולל מיילי ההשקה שיצאו לפני כן. מ-29.7 ואילך הם נקראים. אין תיקון רטרואקטיבי לתקופה שלפני, ואין צורך לגעת בתוכן שכבר נשלח.

**נקודה שרלוונטית עכשיו כשGA4 מותקן:** `handleBuy` ב-`src/routes/course.$slug.tsx` מנווט ל-`/checkout` עם `search: { course: slug }` שדורס את כל הפרמטרים, ו-`checkout.tsx` מריץ `validateSearch` עם סכמת zod שמסננת מפתחות לא מוכרים — כלומר ה-UTM נעלם במעבר לתשלום. **זה לא באג לתיקון**: GA4 מייחס מקור בתחילת סשן, ולכן הנתון כבר נתפס בכניסה לדף הנחיתה. לתעד כדי שלא "יתוקן" בטעות בעתיד.

## התקנת GA4 — מה נעשה בפועל, ומה נשאר בממשק (29.7.2026)

### מה כבר קיים באתר הראשי

`www.ganani.co.il` מריץ סטאק מדידה מלא ובוגר, שנקרא ישירות מהדף החי:

| כלי | מזהה |
|---|---|
| Google Tag Manager | `GTM-NBWG2MQ` |
| GA4 | `G-S7E1GGMBGD` |
| Google Ads | `AW-695737498` |
| Meta Pixel | מותקן (`fbq`) |
| Microsoft Clarity | `n8s9hdebtz` |
| LinkedIn Insight | מותקן |

### ההחלטה שיושמה: אותו נכס, לא נכס חדש

**לא נוצר GA4 property חדש לאתר הקורסים.** הותקן **אותו מזהה מדידה, `G-S7E1GGMBGD`**, עם **מדידה חוצת-דומיינים** בין `www.ganani.co.il` ל-`courses.ganani.co.il`.

הסיבה מעשית: המשפך האמיתי עובר בין שני האתרים (בלוג → דף מכירה → רכישה, או מייל → דף מכירה). נכס נפרד היה מפצל כל מסע כזה לשני סשנים מנותקים ומייחס את הרכישה ל"referral מ-ganani.co.il" במקום למקור האמיתי. `courses.ganani.co.il` הוא תת-דומיין, כך שגם הקוקי משותף — זה התרחיש שחוצה-דומיינים נועד לו.

⚠️ **בלי הגדרת cross-domain, כל הקישורים מהאתר הראשי לקורסים ייספרו כ-self-referral** ויאפסו את הייחוס — זו התקלה הנפוצה ביותר בהתקנה כזו.

### היכן הקוד יושב

`src/lib/analytics.ts` — פונקציית `initAnalytics()` שבודקת את הסכמת העוגיות, ומזריקה את `gtag.js` פעם אחת בלבד (הגנה כפולה: דגל ברמת המודול + בדיקת `id="ga4-script"`). מוגן ב-`typeof window` כי האתר מוגש מהשרת.

ההפעלה נקראת מ-`src/components/cookie-consent.tsx`, ש-`SiteLayout` מרנדר — כלומר היא רצה בכל עמוד, כולל דף הקורס ודף התשלום.

⚠️ **מלכודת שכבר עלתה בנו יום שלם — הסניפט חייב לדחוף `arguments`, לא מערך.** ההתקנה הראשונה נכתבה כך:

```js
function gtag(...args) { window.dataLayer.push(args); }   // ❌ שולח מערך
```

`gtag.js` מזהה פקודות **רק** לפי אובייקט `arguments`; מערך רגיל נחשב אצלו דחיפת נתונים בסגנון GTM ולא פקודה. התוצאה: הסקריפט של גוגל נטען כרגיל, `config` מעולם לא רץ, ואפס נתונים הגיעו — **בלי שום שגיאה בקונסול**. הצורה הנכונה היא זו שבסניפט הרשמי:

```js
function gtag() { window.dataLayer.push(arguments); }     // ✅
```

**איך בודקים שזה באמת עובד**, אם זה יותקן שוב במקום אחר: לא מספיק לראות שתג הסקריפט קיים. בקונסול, `dataLayer.map(x => Object.prototype.toString.call(x))` צריך להחזיר `[object Arguments]` בשתי הרשומות הראשונות, ו-`performance.getEntriesByType('resource')` צריך להראות בקשה ל-`/g/collect` עם `en=page_view`.

**אומת על האתר החי 30.7.2026** — כולל ספירה נפרדת של מעבר בין עמודים, כלומר גם הגדרת "אירועי היסטוריה" פעילה.

**הערה על אימות:** דוח זמן-אמת החזיר אפס גם כשהפניות נשלחו בפועל מדפדפן אוטומטי (headless), ככל הנראה בגלל סינון בוטים של GA4. הדוח הרגיל דווקא הראה את הנתונים. **לאימות אמיתי צריך ביקור מדפדפן אנושי רגיל** — אל תסיקו מאפס בזמן-אמת שההתקנה שבורה.

**ההחלטה שהתקבלה: gtag ישיר, לא GTM.** פשוט יותר, אבל יש לו מחיר שכדאי לזכור — **כל כלי מדידה נוסף בעתיד (פיקסל, Clarity, Ads) ידרוש שינוי קוד ב-Lovable שעולה קרדיטים**, ולא רק הגדרה בממשק GTM.

### באנר העוגיות — מחובר בפועל

`src/components/cookie-consent.tsx` שומר `accepted`/`rejected` ב-`localStorage` תחת `ganani_cookie_consent`, **ומ-29.7 הבחירה באמת שולטת בטעינה**: GA4 נטען רק על `accepted`, מיד עם הלחיצה ובלי רענון, וגם בכל טעינת עמוד עתידית. מי שסירב, או שטרם בחר — לא נטען אצלו כלום. אומת על האתר החי אחרי הפרסום: אפס סקריפטים של גוגל לפני אישור.

מכאן נובע ש**המספרים ב-GA4 הם רצפה, לא ספירה מלאה** — מבקרים שסירבו לעוגיות אינם נמדדים כלל.

⚠️ עוגיות `_ga` נראות ב-`courses.ganani.co.il` גם לפני אישור, אם המבקר ביקר קודם באתר הראשי — הן נקבעות ברמת `.ganani.co.il` ומשותפות לתת-הדומיין. זה לא דליפה מהאתר הזה ולא באג בטעינה המותנית.

### מה זה לא פותר

- **`noindex` אינו קשור.** GA4 עובד מצוין על עמוד שאינו מאונדקס — אין סיבה טכנית לחכות לפתיחת האינדוקס, ויש סיבה טובה שלא: התקנה אחרי 20.8 מפספסת את תחילת רצף ההשקה.
- **Search Console הוא נכס נפרד.** ה-property המוגדר בפרויקט הזה הוא `https://www.ganani.co.il/` בלבד. `courses.ganani.co.il` דורש הוספה נפרדת (נכס `Domain` על `ganani.co.il` דרך רשומת TXT יכסה את שניהם), **ורלוונטי רק אחרי פתיחת האינדוקס ב-17–18.8** — לפני כן הוא רק ידווח "חסום ע"י noindex" על כל עמוד. ההרשאות ב-Search Console הן פר-נכס: חשבון השירות שכבר קורא את `www` יצטרך להתווסף שוב לנכס החדש.
- **המדידה של ההשקה עצמה לא תלויה בזה.** לפי §8 ב-`launch-7-yesodot.md` הבדיקה היא ספירת מכירות מטבלת `orders`. GA4 מוסיף ייחוס לערוץ, לא את המספר עצמו.

### שליפת נתוני GA4 לשיחה

הסקיל [`.claude/skills/google-analytics/`](.claude/skills/google-analytics/SKILL.md) קורא מ-GA4 Data API (קריאה בלבד) — תנועה לפי מקור/קמפיין/עמוד, ודוח זמן-אמת לאימות שהתג יורה. משתמש באותו חשבון שירות של `google-search-console`. דורש `GA4_PROPERTY_ID` ב-`.env`, ושני API מופעלים בפרויקט הענן `hila-seo` (Analytics Data + Analytics Admin).

**GA4 לא יידע מי קנה** — לא נשלח ממנו אירוע `purchase`. ההכנסות נספרות מטבלת `orders`.

### אירוע רכישה — נבדק ונדחה לאחרי ההשקה (החלטת הילה, 30.7.2026)

הדחייה היא בגלל **הבדיקה**, לא בגלל מורכבות: קורס חינמי לא עובר במסלול הזה, ולכן הדרך היחידה לאמת שהאירוע נשלח היא **רכישה אמיתית בתשלום מלא** שמזוכה אחר כך. הילה העדיפה לא לעשות זאת לפני רצף המכירה. לחזור לזה אחרי 10.9.

מה שכבר נבדק, כדי שלא ייחקר שוב:

- **נקודת הירי: `src/routes/order.$ref.tsx`** — עמוד החזרה מ-SUMIT, שכבר מזהה מתי ההזמנה עברה ל-`paid`. `useEffect` **נפרד** עם שמירה משלו נגד ירי כפול. ⚠️ אסור לגעת ב-effect הקיים שמבצע את ה-claim — הוא משתמש בטוקן חד-פעמי, ו-`claimStartedRef` שם קיים בדיוק כדי למנוע ריצה שנייה שתבטל את הראשונה.
- **`getOrderStatus` (ב-`src/lib/payments.functions.ts`) לא מחזיר את הסכום** — רק `status`, `documentUrl` ו-`courseSlugs`. צריך להוסיף ל-select ולהחזרה את `amount_cents` ו-`currency`. זו פונקציית קריאה בלבד, לא נתיב החיוב.
- **אין `window.gtag` באתר.** `analytics.ts` מגדיר את `gtag` מקומית בתוך `initAnalytics` ולא חושף אותה. צריך לייצא משם עוזר לשליחת אירועים — אי אפשר פשוט לקרוא ל-`gtag(...)` מעמוד אחר.
- **אין לגעת ב-`finalizeOrder` וב-`sumit.server.ts`.** כל השינוי בשכבת התצוגה.
- **המספר לעולם לא יתאים ל-`orders`, וזה תקין:** קונה שסגר את הדפדפן לפני טעינת עמוד החזרה, או שסירב לעוגיות, לא ייספר. GA4 נותן ייחוס לערוץ; `orders` נשארת ספירת הכסף.

### מה נשאר להגדיר בממשק GA4 — לא בקוד

שלוש הגדרות שאי אפשר לעשות מהקוד, וכל עוד לא נעשו הנתונים יהיו שגויים:

1. **הדומיינים שלך** (Admin → זרמי נתונים → הזרם → הגדרת התג → הגדרת הדומיינים שלך): תנאי *מכיל* → `ganani.co.il`. מכסה את שני האתרים. **בלי זה כל מעבר בין האתרים נספר כשני סשנים והמקור המקורי נמחק.**
2. **רשימת הפניות לא רצויות** (אותו מסך → הצגת עוד): תנאי *מכיל* → `sumit.co.il`. בלי זה **כל רוכש** נספר כאילו הגיע מ-SUMIT, כי הוא חוזר משם — כלומר דווקא מי ששילם מאבד את הייחוס שלו.
3. **מדידה משופרת** → לוודא ש"שינויי דף על סמך אירועי היסטוריה" דלוק. האתר הוא אפליקציית עמוד יחיד; בלי זה ייספר רק העמוד הראשון בכל ביקור.

### עלות בפועל

שתי הודעות לסוכן של Lovable: 3.8 קרדיטים (דף האודות + הקישורים) ו-2.1 קרדיטים (GA4 + הסרת ה-UTM). לקח שנוסף לזה: **לאגד שינויים לבקשה אחת** — כאן נדרשו שתיים כי החלטת המדידה התקבלה אחרי שהראשונה כבר נשלחה.

## דף אודות וחיבור לאתר הראשי — עלה לאוויר 29.7.2026

הבריף [`courses-site-about-and-linking.md`](courses-site-about-and-linking.md) בוצע ופורסם. מה שקיים עכשיו:

- **דף אודות** ב-`/about` — כותרת "למה ללמוד ממני", טקסט של נגה בשדה `content` בטבלת `site_pages` (slug `about`). ארבעת החלקים הושלמו: שורה בבסיס הנתונים, `src/routes/about.tsx`, `about: "/about"` ב-`PUBLIC_PATHS`, והוספה ל-sitemap (יחד עם `/contact` שהיה חסר שם). **הילה יכולה לערוך את הטקסט בעצמה ב-`/admin/pages`.**
- **בתפריט העליון** (דסקטופ ומובייל): "אודות" ו"לאתר הראשי".
- **בפוטר**: `ganani.co.il` הפך מטקסט חופשי לקישור אמיתי.

⚠️ **הקישורים בין שני האתרים הם בלי UTM, וזה מכוון.** מרגע ששני האתרים מדווחים לאותו נכס GA4 עם מדידה חוצת-דומיינים, הם קישורים פנימיים — ו-UTM על קישור פנימי כותב את המקור מחדש באמצע הסשן ומוחק את מה שבאמת הביא את המבקר. הם נוספו עם UTM ב-29.7 והוסרו באותו יום כשהתקבלה החלטת הנכס האחד. **לא להחזיר.**

מה שנשאר פתוח מהבריף המקורי: **קישור מהוורדפרס לאתר הקורסים** — הכיוון ההפוך, דורש גישה לוורדפרס שלא נבדקה.

## מוצרים

| קורס | מחיר | כתובת |
|---|---|---|
| 7 יסודות שכל עסק צריך כדי לשווק את עצמו | 99 ₪ · עולה ל-129 ב-11.9.2026 | `/course/7-marketing-foundations` |

✅ **279 ₪ המחוק הוסר מהדף ב-3.8.2026** — היה `original_price_cents = 27897` בטבלת `courses`, אופס ל-NULL, ומאומת על האתר החי. הוא היה **נתון, לא קוד**: מרונדר בשתי קופסאות מחיר בתוך תנאי, כך שריקון השדה הפיל אותו משני המקומות בלי שינוי קוד ובלי קרדיטים. אותו טופס (`/admin/course/$courseId`) מחזיק גם את `מחיר (באגורות)`, ולכן גם המעבר ל-129 ב-11.9 הוא פעולת ממשק.

⚠️ **`src/routes/course.$slug.tsx` משותף לכל הקורסים.** ל-ManyChat יש עוגן מחיר אמיתי (₪890 מול ₪1290), ולכן כל טקסט מחיר שנוסף לדף חייב להיות שדה פר-קורס ולא קבוע בקוד. נותר שינוי קוד אחד פתוח (תווית `מחיר מבצע:`, ניסוח הגישה, ושורת דדליין דרך עמודה חדשה `price_note`) — הבקשה המלאה מוכנה ב-`launch-7-yesodot.md` §1.

9 שיעורי וידאו. **השיעור הראשון פתוח לצפייה חינם בדף המכירה, בלי הרשמה ובלי כרטיס אשראי.** ביטול תוך 14 יום כל עוד לא נצפו שיעורים.

## מה עוד יש ב-Lovable

- `one-on-one-meetings-hila-ganani` — דף פגישת ייעוץ (`b3705fe9-8e3d-457a-8fb0-7fe2fb661874`)
- `מסע הלקוח המוזהב` — תרשים זרימה, לא מפורסם (`c78ba64f-690e-4b05-af44-9713f34796cf`)
