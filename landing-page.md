# דף הנחיתה "אבחון שיווקי לעסק" — מסמך התמצאות

דף הנחיתה **אינו** בתיקייה הזו. הוא פרויקט Lovable נפרד, כמו אתר הקורסים. הקובץ הזה קיים כדי שלא יהיה צורך לשחזר מאיפה הכל מגיע — הוא מפנה, לא מתעד את המערכת עצמה.

⚠️ **יש כאן שני פרויקטים נפרדים ודומים מאוד. אסור לבלבל ביניהם.** אחד חי בפרודקשן, השני הוא גרסת העבודה.

## שני הפרויקטים

| | 🔴 מקורי (פרודקשן) | ✅ עותק עבודה |
|---|---|---|
| שם ב-Lovable | `one-on-one-meetings-hila-ganani` | `landing-1on1-WORKING-COPY` |
| Project ID | `b3705fe9-8e3d-457a-8fb0-7fe2fb661874` | `197bfe8a-c6c1-4f24-9da2-268dcaecb569` |
| כתובת חיה | `landing.ganani.co.il` | `ganani-diagnosis-qa.lovable.app` (QA בלבד) |
| Supabase | `hqiurojqvlubrzraeeoh` — **17 לידים אמיתיים** | `aufveudyyqixmisiajdi` — נפרד לגמרי |
| מה מוכר | פגישת ייעוץ 90 דק', 1,200 ₪ | אבחון שיווקי לעסק, 1,800 ₪ |
| Make webhook | **מחובר ופעיל** | **אין. הוסר מהקוד** |

שניהם ב-workspace `SDhpfFncRNZhwHGbc7jS` ("Hila's Lovable"), שניהם TanStack Start (TypeScript) + Tailwind + shadcn/ui, מוגשים SSR.

עורך העותק: `https://lovable.dev/projects/197bfe8a-c6c1-4f24-9da2-268dcaecb569`

## 🔴 שלושה דברים שיישברו אם לא ייזהרו

**1. ה-webhook של Make מקודד קשיח בפרויקט המקורי.**
`hook.eu1.make.com/re566s9de41et4ue3bx7tj4issf5f9cc` — כל שליחת טופס במקור מפעילה תרחיש אמיתי ויוצרת ליד אמיתי. **בעותק הוא הוסר לחלוטין מהקוד** (אומת: אפס אזכורים בכל קבצי ה-JS המפורסמים). אם משכפלים שוב מהמקור — הוא חוזר.

**2. 17 הלידים הקיימים יישארו בפרויקט המקורי.**
ברגע שהדומיין יצביע על העותק, לידים חדשים ייכנסו לבסיס נתונים אחר. צריך להחליט מראש: להעביר, או להשאיר את המקור כארכיון ולתעד איפה ההיסטוריה.

**3. canonical, og:url, robots ו-sitemap.**
במקור שלושתם מצביעים ל-`one-on-one-meetings-hila-ganani.lovable.app` ולא לדומיין האמיתי. **בעותק ה-canonical וה-og:url הוסרו בכוונה** עם הערות בקוד — הם צריכים להיקבע לדומיין הפרודקשן ביום המעבר, לא לפני. `robots.txt` וה-sitemap בעותק עדיין מצביעים לכתובת של המקור וטרם עודכנו.

## מבנה הדף

13 סקשנים עם `id`, בסדר הזה:

`hero` · `cause` · `about` · `diagnosis` · `prep` · `deliverable` · `case-swim` · `case-courses` · `testimonial` · `fit` · `offer` · `faq` · `final-cta`

המסר המרכזי: **"אתם רואים את התוצאה. אני מחפשת את הסיבה."** — הילה לא מאשרת ללקוח את מה שהוא כבר חושב, היא בודקת אם האבחנה שלו נכונה.

**עובדות שאסור לשנות בניסוח:**
- המחיר הוא `1,800 ₪ + מע"מ`.
- שורת התוצאה בקייס השני היא **מילה במילה**: `בהמשך הפעילות הגיעה לכ-150,000 ₪ בחודש אחרי קיזוז הוצאות הפרסום.` — ההסתייגות "אחרי קיזוז" חייבת להישאר צמודה למספר וקריאה. **לעולם לא לכתוב "רווח נקי".**
- ההמלצה היחידה היא של **גנית פינטו**. אין לה תפקיד או חברה — לא נמסרו.
- **הרכיב `Proof()` על ההמלצות המומצאות ו-"+218%" נמחק.** אם הוא צץ שוב משכפול — הוא לא אמיתי.

## מסלול ההמרה

```
CTA ראשי → Popup עם טופס → INSERT ל-Supabase של העותק → /thank-you
```

ארבעה כפתורי `אני רוצה לקבוע אבחון שיווקי` (hero, אמצע, offer, final) פותחים את אותו Popup. אין ניווט לעמוד אחר.

**הטופס:** שם · טלפון · אימייל · אתר/עמוד עסק (לא חובה) · "מה גורם לך לחפש אבחון שיווקי עכשיו" · checkbox דיוור.

⚠️ **אישור הדיוור אינו תנאי לשליחה** — לא מסומן מראש, ולא חוסם. נשמר כ-true/false בכל מקרה. זה מכוון.

הכפתור אומר `שליחת הפרטים` ולא "קביעת אבחון", כי בשלב הזה לא נקבעת פגישה.

### טבלת `leads` בעותק — 16 עמודות

| קבוצה | עמודות |
|---|---|
| מזהה | `id`, `created_at` |
| הטופס | `full_name`, `email`, `phone`, `website_url`, `inquiry_reason`, `marketing_consent` |
| ייחוס | `utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `utm_term`, `gclid`, `fbclid` |
| שאריות | `business_info` — מהטופס הישן, ריקה ולא בשימוש. לא נמחקה בכוונה |

`inquiry_reason` היא עמודה נפרדת ולא מיחזור של `business_info`, כי המשמעות שונה ("למה עכשיו" מול "ספרו לי על העסק").

**RLS:** INSERT פתוח לאנונימי (כדי שהטופס יעבוד), SELECT רק למשתמש מאומת. כלומר **הלידים אינם קריאים פומבית**. אין לשנות.

## מדידה — scaffold בלבד

אין GA4, אין GTM, אין Meta Pixel, אין Clarity. **אף מזהה מדידה לא הותקן.** מה שכן קיים:

- `src/lib/tracking.ts` — דוחף אירועים ל-`window.dataLayer` בלבד. אירועים מוגדרים: `cta_primary_click`, `cta_whatsapp_click`, `header_cta_click`, `booking_start`, `form_start`, `form_submit`, `purchase_completed` (מיוצא, לא נקרא — אין checkout).
- `src/lib/utm.ts` — שומר UTM + `gclid`/`fbclid` ב-`sessionStorage` תחת `ganani_utm`, **first-touch** (לא דורס בניווט הבא), ומצרף אותם לכל אירוע ולכל ליד.

`form_start` נורה באינטראקציה הראשונה עם שדה, **לא בפתיחת ה-Popup**. `form_submit` רק אחרי שמירה מוצלחת.

**כשיתחברו לגוגל:** לשקול את אותו נכס של שאר האתרים, `G-S7E1GGMBGD` — אחרת הדף יישאר מחוץ למדידה חוצת-הדומיינים. ראו את המלכודת המתועדת ב-[`courses-platform.md`](courses-platform.md): הסניפט חייב לדחוף `arguments`, לא מערך.

## מה מכוון שלא מחובר

`src/lib/cta.ts` מחזיק `WHATSAPP_URL = ""` ריק. הכפתורים המשניים (`יש לי שאלה לפני שקובעים`) בנויים ולא מנווטים לשום מקום — **אין מספר וואטסאפ מאומת**, ולא הומצא אחד.

אין SUMIT, אין לינק תשלום, אין מערכת תיאום פגישות, ואין שום דבר מהתהליך שאחרי הרכישה — לא שאלון הכנה, לא איסוף נתונים, לא אוטומציות. הכל בכוונה, לטיפול נפרד.

## נכסים ומשקל

| | לפני | אחרי |
|---|---|---|
| תמונת הילה | `hila.png` 1,304KB | `hila.webp` 600×718, **30.7KB** |
| לוגו | `logo.jpg` 70KB, רקע לבן אטום | `logo.png` שקוף 886×254, **18.6KB** |
| משקל הדף | 2,134KB | **~817KB** |

הלוגו השקוף נלקח מ-`merav/reference/logo/hila-ganani-logo-black.png` — הגרסה הרשמית, באותן מידות בדיוק כמו ה-JPG הישן. יש גם גרסה לבנה לרקע כהה, לא בשימוש כרגע. **אין לייצר מחדש לוגו ואין להסיר רקע ב-AI.**

הפלטה זהה למקור. `--primary` הוא `#46278f`. הצבע הכהה `#140a29` (הטוקן `--foreground`) משמש כמשטח בקייס השני — זה **הרגע הכהה היחיד באמצע הדף** והוא מכוון.

## מה נשאר לפני production

1. לחבר `WHATSAPP_URL` ולהחליט איך נמכר האבחון (SUMIT? מערכת תיאום?)
2. להגדיר canonical ו-og:url לדומיין האמיתי — בשני קבצים
3. לעדכן `robots.txt` ו-sitemap — עדיין מצביעים לפרויקט המקורי
4. להחליט על 17 הלידים במקור
5. להתקין מדידה + שלוש ההגדרות בממשק GA4 (דומיינים, הפניות לא רצויות, אירועי היסטוריה)
6. תמונת OG — עדיין מהמיצוב הישן ומתארחת חיצונית אצל Lovable
7. להסיר את באנר "Edit with Lovable" שמוזרק לדף המפורסם
8. `/thank-you` עודכן לשירות החדש, אבל חסום ב-`robots.txt` — לוודא שזה עדיין רצוי

## ליקויים ידועים שלא תוקנו

- **שטח ריק מתון בפינה השמאלית-תחתונה של ההירו**, לצד בלוק "השירות".
- **שני הקייסים כבר לא זוג ויזואלי** — הראשון בהיר ושקט, השני רצועה כהה. הניגוד הרעיוני עובד, האיזון פחות.
- **הקצב הכללי עדיין חיוור** — הרקע הבהיר הוא ~60% מהדף, ושני הרקעים הבהירים נבדלים ביחס של 1.11:1 בלבד. הרגע הכהה פתר את האמצע, לא את האחידות.
- המרכאה הדקורטיבית בהמלצה מוגדרת ב-`font-serif` — הפונט היחיד בדף שאינו Heebo.
