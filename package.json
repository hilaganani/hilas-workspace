{
  "name": "creative-agent-workspace",
  "version": "1.0.0",
  "type": "module",
  "description": "CreativeAgent workspace - Roi (CEO) orchestrates a content team; Merav generates content images via Nano Banana 2 (Gemini)",
  "scripts": {
    "generate": "node scripts/generate.mjs"
  },
  "dependencies": {
    "@google/genai": "^0.7.0",
    "dotenv": "^16.4.5"
  }
}
