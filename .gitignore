# Secrets
.env

# Generated artifacts (per CLAUDE.md convention)
/output/

# Liat's research working data (search memory + saved sources)
/liat/
/Content/

# Noga's style guide + writing reference samples (personal/local, not committed)
/noga/

# Merav's working directory (style references + generated image outputs)
/merav/

# Dani's SEO output packages
/dani/

# Yael's strategy doc + monthly content plans
/yael/

# Gefen's profile-structure recommendations
/gefen/

# Node (kept in case a future employee needs it; nothing in the repo uses npm right now)
node_modules/

# Local machine-specific Claude Code settings
.claude/settings.local.json

# macOS folder metadata
.DS_Store
