# Secrets
.env
/credentials/

# Generated artifacts (per CLAUDE.md convention)
/output/

# Liat's research working data (search memory + saved sources)
/liat/
/Content/

# Noga's style guide + writing reference samples (personal/local, not committed)
/noga/*
!/noga/style-guide.md

# Merav's working directory (style references + generated image outputs)
/merav/

# Dani's SEO output packages
/dani/

# Yael's strategy doc + monthly content plans
/yael/

# Gefen's profile-structure recommendations
/gefen/

# Tamar's pricing rules, Prospero proposal archive, and proposal drafts
# (real client names, prices and terms — local only)
/tamar/

# Node (kept in case a future employee needs it; nothing in the repo uses npm right now)
node_modules/

# Local machine-specific Claude Code settings
.claude/settings.local.json

# macOS folder metadata
.DS_Store
.worktrees/

# Measurement system for the "7 יסודות" funnel — procedure, collection scripts,
# snapshots and the spreadsheet. Untracked-and-not-ignored files are exactly what
# `git clean -fd` removes; this whole directory was lost that way on 26.8.2026.
/measurement/

# Zadarma call recordings — real customer phone numbers in the filenames and
# actual recorded conversations. This repo is public; these must never be pushed.
/zadarma/
